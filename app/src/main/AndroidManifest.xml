<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.raizen.fighter">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning,UnusedAttribute">
        <activity android:name="com.raizen.fighter.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.raizen.fighter.activities.EntertaimentActivity"
            android:screenOrientation="fullSensor" />
        <activity
            android:name="com.raizen.fighter.activities.HealthActivity"
            android:screenOrientation="fullSensor" />
        <activity
            android:name="com.raizen.fighter.activities.BusinessActivity"
            android:screenOrientation="fullSensor" />
        <activity
            android:name="com.raizen.fighter.activities.TechnologyActivity"
            android:screenOrientation="fullSensor" />
        <activity
            android:name="com.raizen.fighter.activities.HeadLineActivity"
            android:screenOrientation="fullSensor" />
        <activity
            android:name="com.raizen.fighter.activities.SportsNewsActivity"
            android:screenOrientation="fullSensor" />
        <activity
            android:name="com.raizen.fighter.activities.OpenNewsActivity"
            android:screenOrientation="fullSensor" />
    </application>

</manifest>